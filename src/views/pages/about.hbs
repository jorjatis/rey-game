<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
</head>
<body>
  Esto es el about

  <p>Listado:</p>

  {{> test-each }}

  {{#each items}}
      <li>
        <h2>{{name}}</h2>
        <p>{{description}}</p>
      </li>
    {{/each}}
</body>
</html>